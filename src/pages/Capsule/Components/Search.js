import { Box } from 'rebass/styled-components';

export default () => (
    <Box>
        <input
            type="text"
            style={{
                borderRadius: '14px',
                height: '28px',
                border: '1px solid #000000',
            }}
        />
    </Box>
);
